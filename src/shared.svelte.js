/* eslint-disable no-undef */
import { START_PAGE } from './const';

export const _state = $state({
    sounds: true,
    page: START_PAGE,
    input: [],
    letter_box_size: 0,
    prompt: null,
    show_prompt: false,
});
